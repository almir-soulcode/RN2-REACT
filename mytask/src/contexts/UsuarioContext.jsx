import { createContext } from "react";

// Define um contexto na aplicação
// ou seja, um mecanismo para compartilhar
// dados entre os componentes da aplicação.
export const UsuarioContext = createContext();